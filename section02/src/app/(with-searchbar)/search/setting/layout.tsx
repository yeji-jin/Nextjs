import { ReactNode } from "react";

export default function SearchSettingLayout({ children }: { children: ReactNode }) {
  return (
    <div>
      <div> SearchSettingLayout</div>
      <hr />
      {children}
    </div>
  );
}
